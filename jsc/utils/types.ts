export enum ParamType {
    t_address,
    t_bool,
    t_number,
    t_string
}

export enum ProposalState {
    Active,
    Canceled,
    Defeated,
    Succeeded,
    Queued,
    Expired,
    Executed
}

export enum VoteType {
    Against,
    For,
    Abstain
}
